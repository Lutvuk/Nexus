POSTGRES_URL="host=localhost user=postgres password=nexus dbname=nexus port=5432 sslmode=disable"
PORT=8080
JWT_SECRET="change-me-in-prod"
# Optional: set to your frontend origin (e.g. https://app.example.com). Use "*" for public API without credentials.
CORS_ALLOW_ORIGIN="*"

# SMTP (local free testing via Mailpit)
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USERNAME=""
SMTP_PASSWORD=""
SMTP_FROM="no-reply@nexus.local"
